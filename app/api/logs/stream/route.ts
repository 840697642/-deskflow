import { NextRequest } from 'next/server'; import { getMockStore } from '@/lib/mock-store'; export async function GET(r:NextRequest){const enc=new TextEncoder();const body=new ReadableStream({start(c){for(const log of getMockStore().logs.slice(0,3))c.enqueue(enc.encode(`event: log\ndata: ${JSON.stringify(log)}\n\n`));c.enqueue(enc.encode('event: done\ndata: {}\n\n'));c.close()}});return new Response(body,{headers:{'Content-Type':'text/event-stream','Cache-Control':'no-cache'}})}
