import { NextRequest } from 'next/server'; import { jsonError,jsonSuccess } from '@/lib/api/response'; import { getMockStore } from '@/lib/mock-store'; import { parseObject } from '@/lib/knowledge-api'; export async function POST(r:NextRequest){const p=await parseObject(r);if(p.error)return p.error;const b=p.value!;if(!b.settings||typeof b.settings!=='object')return jsonError(r,400,'INVALID_REQUEST','settings 无效',false);Object.assign(getMockStore().settings,b.settings);return jsonSuccess(r,{imported:true})}
