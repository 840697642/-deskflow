import { NextRequest } from 'next/server'; import { jsonError,jsonSuccess } from '@/lib/api/response'; import { getMockStore } from '@/lib/mock-store'; import { upsertError } from '@/lib/knowledge-api'; export async function POST(r:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;const log=getMockStore().logs.find(x=>x.id===id);if(!log)return jsonError(r,404,'NOT_FOUND','日志不存在',false);const error=upsertError(`space-${log.projectId}`,log.message);return jsonSuccess(r,error,{status:201})}
