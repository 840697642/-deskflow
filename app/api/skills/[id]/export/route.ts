import { NextRequest } from 'next/server'; import { jsonSuccess } from '@/lib/api/response'; import { getMockStore } from '@/lib/mock-store'; export async function GET(r:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;const s=getMockStore().skills.find(x=>x.id===id);return jsonSuccess(r,{filename:'SKILL.md',content:`# ${s?.name??'Skill'}\n\n${s?.description??''}`})}
