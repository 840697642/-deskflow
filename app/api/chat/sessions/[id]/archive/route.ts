import { NextRequest } from 'next/server'; import { jsonError,jsonSuccess } from '@/lib/api/response'; import { getMockStore } from '@/lib/mock-store'; export async function POST(r:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;const s=getMockStore().chatSessions.find(x=>x.id===id);if(!s)return jsonError(r,404,'NOT_FOUND','会话不存在',false);s.updatedAt=new Date().toISOString();return jsonSuccess(r,{archived:true,sessionId:id})}
