import { NextRequest } from 'next/server'; import { jsonError,jsonSuccess } from '@/lib/api/response'; import { getMockStore } from '@/lib/mock-store'; export async function POST(r:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;const s=getMockStore().skills.find(x=>x.id===id);if(!s)return jsonError(r,404,'NOT_FOUND','Skill 不存在',false);s.structure=['输入解析','执行步骤','输出校验'];return jsonSuccess(r,s)}
