import { NextRequest } from 'next/server'; import { jsonError, jsonSuccess } from '@/lib/api/response'; import { getMockStore } from '@/lib/mock-store'; export async function GET(request: NextRequest) { const raw = Number(new URL(request.url).searchParams.get('days') ?? '7'); if (!Number.isInteger(raw) || raw < 1 || raw > 31) return jsonError(request, 400, 'INVALID_QUERY', 'days 必须为 1-31 的整数', false); return jsonSuccess(request, getMockStore().toolTimeDays.slice(0, raw)) }
