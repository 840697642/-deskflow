import { NextRequest } from 'next/server'; import { jsonError, jsonSuccess } from '@/lib/api/response'; import { getMockStore } from '@/lib/mock-store'; export async function POST(request: NextRequest, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; const item = getMockStore().inboxItems.find((entry) => entry.id === id); if (!item) return jsonError(request, 404, 'NOT_FOUND', `收件箱条目 ${id} 不存在`, false); if (item.status !== 'pending') return jsonError(request, 400, 'INVALID_STATUS', '只能忽略 pending 状态的条目', false); item.status = 'dismissed'; return jsonSuccess(request, item) }
