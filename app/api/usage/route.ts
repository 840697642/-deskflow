import { NextRequest } from 'next/server'; import { jsonError, jsonSuccess } from '@/lib/api/response'; import { getMockStore } from '@/lib/mock-store'; export async function GET(request: NextRequest) { const p = new URL(request.url).searchParams; const from = p.get('from'); const to = p.get('to'); const tool = p.get('tool'); if (tool && !['video', 'game', 'app'].includes(tool)) return jsonError(request, 400, 'INVALID_TOOL', 'tool 不是有效值', false); return jsonSuccess(request, getMockStore().usageRecords.filter((record) => (!from || record.day >= from) && (!to || record.day <= to) && (!tool || record.toolId === tool))) }
