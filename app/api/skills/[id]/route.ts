import { NextRequest } from 'next/server'; import { jsonError,jsonSuccess } from '@/lib/api/response'; import { getMockStore } from '@/lib/mock-store'; import { parseObject } from '@/lib/knowledge-api'; export async function GET(r:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;const s=getMockStore().skills.find(x=>x.id===id);return s?jsonSuccess(r,s):jsonError(r,404,'NOT_FOUND','Skill 不存在',false)} export async function PATCH(r:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;const s=getMockStore().skills.find(x=>x.id===id);if(!s)return jsonError(r,404,'NOT_FOUND','Skill 不存在',false);const p=await parseObject(r);if(p.error)return p.error;Object.assign(s,p.value);return jsonSuccess(r,s)}
