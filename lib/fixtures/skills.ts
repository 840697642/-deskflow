import type { Skill } from '@/lib/types/skills'
const t = '2026-09-04T10:00:00Z'
export const mockSkills: Skill[] = Array.from({ length: 6 }, (_, i) => ({ id: `skill-${i + 1}`, name: ['代码审查', '视频分镜', '错误诊断', '数据整理', '图像提示词', '进化工作流'][i], slug: `skill-${i + 1}`, description: '用于工作台自动化的可复用技能', output: (['code', 'video', 'text', 'data', 'image', 'code'] as const)[i], domains: [(['app', 'video', 'app', 'general', 'video', 'game'] as const)[i]], tags: ['v0.8', i % 2 ? '自动化' : '生产力'], structure: ['输入解析', '执行步骤', '输出校验'], useCases: ['工作台任务'], usage: '输入需求后运行该 Skill', pinned: i < 2, usageCount: 10 - i, usageTrend: [1, 2, 2, 3, 1, 2, 3], successRate: 92 - i * 4, lastUsedAt: t, addedAt: t, source: { kind: i === 5 ? 'evolved' : 'local', label: i === 5 ? '融合版本' : '本地库', linked: i === 0, version: '1.0.0', upstreamVersion: i === 0 ? '1.1.0' : undefined }, compat: { codex: true, cursor: true, trae: true, claude: true }, permissions: [{ label: i === 2 ? '执行命令' : '读取文件', risk: i === 2 ? 'high' : 'low' }], version: '1.0.0', parentIds: i === 5 ? ['skill-1', 'skill-3'] : undefined, showcase: [], goldenCases: 3, enabled: i !== 2, status: i === 5 ? 'draft' : 'active' }))
export const MOCK_SKILLS = mockSkills
