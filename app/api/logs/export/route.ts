import { NextRequest } from 'next/server'; import { jsonSuccess } from '@/lib/api/response'; import { getMockStore } from '@/lib/mock-store'; export async function GET(r:NextRequest){const p=new URL(r.url).searchParams;const logs=getMockStore().logs.filter(x=>p.get('include_debug')==='1'||x.level!=='debug');return jsonSuccess(r,p.get('format')==='ndjson'?logs.map(x=>JSON.stringify(x)).join('\n'):logs)}
