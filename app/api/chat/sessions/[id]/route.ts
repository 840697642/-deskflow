import { NextRequest } from 'next/server'; import { jsonError,jsonSuccess } from '@/lib/api/response'; import { getMockStore } from '@/lib/mock-store'; import { parseObject } from '@/lib/knowledge-api'; export async function PATCH(r:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;const s=getMockStore().chatSessions.find(x=>x.id===id);if(!s)return jsonError(r,404,'NOT_FOUND','会话不存在',false);const p=await parseObject(r);if(p.error)return p.error;Object.assign(s,p.value);s.updatedAt=new Date().toISOString();return jsonSuccess(r,s)}
