import { NextRequest } from 'next/server'; import { jsonSuccess } from '@/lib/api/response'; import { getMockStore } from '@/lib/mock-store'; export async function POST(r:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;const s=getMockStore().skills.find(x=>x.id===id);if(s&&s.source.upstreamVersion){s.version=s.source.upstreamVersion;s.source.upstreamVersion=undefined}return jsonSuccess(r,s)}
